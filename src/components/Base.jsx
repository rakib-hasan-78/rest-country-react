import React from 'react';
import Header from './Header';
import Search from './Search';

const Base = () => {
    return (
        <section>
        <Header />
        <Search />
        </section>
    );
};

export default Base;