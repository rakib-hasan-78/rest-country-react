import React from 'react';
import Header from './Header';
import Countries from './Countries';
const Base = () => {
    return (
        <section>
        <Header />
        <Countries />
        </section>

    );
};

export default Base;